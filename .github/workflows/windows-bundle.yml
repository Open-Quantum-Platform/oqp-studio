name: Windows standalone bundle
# Builds a self-contained Windows distribution: unzip and run openqp.exe.
# No Python, no oneAPI, no MKL install on the user's machine -- the bundle
# carries the interpreter, liboqp.dll, the DFT-D4 DLLs, the Intel Fortran/OpenMP
# redistributables, MKL, and the basis-set data.
#
# MKL is carried INSIDE this archive, which is the one difference from the
# Windows wheel.  Both link the same MKL ILP64, but libmkl_core alone exceeds
# PyPI's per-file size limit, so the wheel excludes every mkl_*.dll from its
# repair and declares `mkl` as a pip dependency instead.  A zip has no such
# limit and no package manager to lean on, so it ships the DLLs: the whole
# point is that the user installs nothing.
#
# (The Linux wheels differ again -- they bundle an ILP64 OpenBLAS, because
# there is no pip-installable MKL story that fits the manylinux policy.)
on:
  # Weekly, so a broken engine build surfaces within days rather than at the
  # next app release, and on demand.  A published OQP Studio release rebuilds
  # and attaches the archives it ships with.
  schedule:
    - cron: "0 3 * * 0"   # Sundays, 03:00 UTC
  workflow_dispatch:
    inputs:
      openqp_ref:
        description: "OpenQP tag to build (default: its latest release)"
        required: false
        type: string
  release:
    types: [published]
  workflow_call:
    inputs:
      release_tag:
        description: "Attach the archive to this already-published release"
        required: false
        type: string
      openqp_ref:
        description: "OpenQP tag to build (default: its latest release)"
        required: false
        type: string

# `gh release upload` needs write access to contents, and the default workflow
# token is read-only.  This is the only write these workflows do -- the archive
# is attached to an already-published release, nothing else is touched.
permissions:
  contents: write

jobs:
  bundle:
    name: openqp-windows-x86_64 (standalone)
    runs-on: windows-2025
    timeout-minutes: 240
    defaults:
      run:
        shell: pwsh
    steps:
      # This repository carries the build script and the composite action the
      # steps below use, so it has to be on disk; the engine is checked out
      # separately, into openqp/.
      - name: Check out this repository
        uses: actions/checkout@v4

      - name: Resolve OpenQP's latest release
        id: engine
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # This repository ships the desktop application; the compute engine
          # is a separate project.  Build a released engine, never a moving
          # branch: whatever a user runs from this archive should be a version
          # OpenQP itself has verified and tagged.
          $tag = "${{ inputs.openqp_ref }}"
          if (-not $tag) {
            $tag = gh release view --repo Open-Quantum-Platform/openqp --json tagName -q .tagName
          }
          "tag=$tag" >> $env:GITHUB_OUTPUT
          Write-Host "Building OpenQP $tag"

      - name: Check out OpenQP
        uses: actions/checkout@v4
        with:
          repository: Open-Quantum-Platform/openqp
          ref: ${{ steps.engine.outputs.tag }}
          path: openqp
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up Intel oneAPI
        uses: ./.github/actions/setup-oneapi-windows

      - name: Build and install OpenQP
        env:
          CC: icx
          CXX: icx
          FC: ifx
          CMAKE_GENERATOR: Ninja
          CMAKE_BUILD_PARALLEL_LEVEL: 4
          OQP_EXTERNALS_ROOT: C:\oqpx
        run: |
          # PowerShell reports only the LAST native command's exit code as the
          # step's status, so a failing build here used to be reported green and
          # the error only surfaced later, as a missing DLL.  These two make any
          # non-zero exit stop the step where it happened.
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $true
          New-Item -ItemType Directory -Force -Path C:\oqpx, C:\oqpb | Out-Null
          python -m pip install --upgrade pip
          python -m pip install cmake ninja numpy cffi scikit-build-core pyinstaller

          # OpenQP patches mctc-lib with whatever `patch` CMake finds first.
          # On this runner that can be Strawberry Perl's GNU patch 2.5.9,
          # which does not fail on our patch -- it CRASHES on it:
          #   Assertation failed! ... patch-2.5.9-src\patch.c, Line 354
          # and the crash is reported as "source matches neither side of
          # patch", failing the build.  Which of the two patch.exe on the
          # image comes first depends on PATH, so the same commit built
          # yesterday and did not today.  Git for Windows ships GNU patch
          # 2.7.6; name it explicitly and prove it runs before the hour-long
          # build starts.
          $patch = "C:\Program Files\Git\usr\bin\patch.exe"
          if (-not (Test-Path $patch)) { throw "no usable patch.exe: $patch" }
          & $patch --version | Select-Object -First 1

          python -m pip install -v .\openqp --no-build-isolation `
            --config-settings=build-dir=C:\oqpb `
            "--config-settings=cmake.define.OQP_DFTD4_PATCH_EXECUTABLE=$patch" `
            --config-settings=cmake.define.LINALG_LIB=Intel10_64ilp `
            --config-settings=cmake.define.ENABLE_MPI=OFF `
            --config-settings=cmake.define.ENABLE_OPENMP=ON `
            --config-settings=cmake.define.ENABLE_DDX=ON `
            --config-settings=cmake.define.USE_LIBINT=OFF `
            --config-settings=cmake.define.ENABLE_OPENTRAH=OFF `
            --config-settings=cmake.define.BUILD_TESTING=OFF
          python -m pip install scipy "numpy<2.2" basis_set_exchange

          # Fail here, where the build log is, rather than three steps later
          # with an unhelpful "missing DLL".
          python -c "import oqp, os; print('oqp at', os.path.dirname(oqp.__file__))"

      - name: Freeze into a standalone directory
        run: |
          # PowerShell reports only the LAST native command's exit code as the
          # step's status, so a failing build here used to be reported green and
          # the error only surfaced later, as a missing DLL.  These two make any
          # non-zero exit stop the step where it happened.
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $true
          # `import oqp` prints "Failed to import mpi4py" to stdout on a non-MPI
          # build, so take only the last line -- the path.
          $pkg = (python -c "import os, oqp; print(os.path.dirname(oqp.__file__))") | Select-Object -Last 1
          Write-Host "package: $pkg"

          # pip installs a launcher .exe, not a -script.py, so give PyInstaller
          # its own entry point.
          $entry = "C:\oqpwork\openqp_entry.py"
          New-Item -ItemType Directory -Force -Path C:\oqpwork | Out-Null
          @(
            'import sys'
            'from oqp.pyoqp import main'
            'if __name__ == "__main__":'
            '    sys.exit(main())'
          ) | Set-Content -Encoding UTF8 $entry

          # The native DLLs are loaded through cffi at run time and are
          # therefore invisible to PyInstaller's static analysis; add them by
          # name.  Discover them rather than assuming a layout: the Intel
          # redistributables and the MKL kernels are versioned differently
          # between oneAPI releases.
          $dlls = @()
          $dlls += Get-ChildItem "$pkg\lib\*.dll" -ErrorAction SilentlyContinue
          # ddX is external until OpenQP's native continuum solver is merged.
          # Its build tree is not guaranteed to be copied into the wheel, so
          # locate and add it explicitly before freezing the application.
          $ddx = Get-ChildItem -Path "$pkg\lib", "C:\oqpb", "C:\oqpx" -Filter "*ddx*.dll" -File -Recurse -ErrorAction SilentlyContinue
          if (-not $ddx) { throw "external ddX was enabled but its DLL was not built" }
          $dlls += $ddx
          foreach ($pat in 'libifcoremd*.dll','libifportmd*.dll','libmmd*.dll',
                           'libiomp5md*.dll','svml_dispmd*.dll','libirngmd*.dll',
                           'libintlc*.dll','libimalloc*.dll') {
            $dlls += Get-ChildItem -Path "$env:CMPLR_ROOT" -Filter $pat -Recurse -ErrorAction SilentlyContinue
          }
          # MKL: the interface/threading/core trio and the per-ISA kernels it
          # dispatches to at run time.  Everything OpenQP never calls -- SYCL
          # offload, ScaLAPACK/BLACS/CDFT (no MPI here), the TBB and sequential
          # threading layers, the PGI and F95 shims -- is left out; keeping them
          # would roughly triple the download for no benefit.
          $mklSkip = 'mkl_sycl','mkl_scalapack','mkl_blacs','mkl_cdft','mkl_pgi',
                     'mkl_tbb_thread','mkl_sequential','mkl_lapack95','mkl_blas95'
          $dlls += Get-ChildItem -Path "$env:MKLROOT" -Filter "mkl_*.dll" -Recurse -ErrorAction SilentlyContinue |
                   Where-Object { $n = $_.Name; -not ($mklSkip | Where-Object { $n -like "$_*" }) }
          $dlls = $dlls | Sort-Object FullName -Unique
          if (-not $dlls) { throw "no runtime DLLs discovered" }
          Write-Host ("discovered {0} DLLs, {1:N0} MB" -f $dlls.Count, (($dlls | Measure-Object Length -Sum).Sum/1MB))
          $dlls | ForEach-Object { Write-Host ("  {0,-32} {1,8:N1} MB" -f $_.Name, ($_.Length/1MB)) }
          # Version-agnostic: MKL suffixes its DLLs with a major version that
          # changes between releases (mkl_core.2.dll -> mkl_core.3.dll).
          foreach ($pat in 'liboqp.dll','dftd4.dll','mctc-lib.dll','multicharge.dll',
                           '*ddx*.dll','mkl_core.*.dll','mkl_intel_thread.*.dll','libifcoremd.dll') {
            if (-not ($dlls.Name | Where-Object { $_ -like $pat })) {
              throw "required DLL missing from the bundle: $pat"
            }
          }

          $addBin = $dlls | ForEach-Object { "--add-binary=`"$($_.FullName);oqp/lib`"" }
          $cmd = @(
            'python -m PyInstaller --noconfirm --clean --onedir --name openqp',
            '--distpath C:\oqpdist --workpath C:\oqpwork --specpath C:\oqpwork',
            '--collect-data oqp --collect-submodules oqp',
            '--collect-data basis_set_exchange --collect-submodules basis_set_exchange',
            '--collect-submodules scipy --collect-submodules numpy',
            # basis_set_exchange asks importlib.metadata for its own version at
            # import time, which needs the .dist-info that --collect-data does
            # not carry.  Recursive so its dependencies are covered too.
            '--recursive-copy-metadata basis_set_exchange',
            '--copy-metadata OpenQP',
            ($addBin -join ' '),
            "`"$entry`""
          ) -join ' '
          Invoke-Expression $cmd
          if (-not (Get-ChildItem C:\oqpdist\openqp -Filter "*ddx*.dll" -File -Recurse -ErrorAction SilentlyContinue)) {
            throw "frozen bundle is missing external ddX"
          }

      - name: Add the data tree and a README
        run: |
          # PowerShell reports only the LAST native command's exit code as the
          # step's status, so a failing build here used to be reported green and
          # the error only surfaced later, as a missing DLL.  These two make any
          # non-zero exit stop the step where it happened.
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $true
          $ver = python -c "import tomllib,pathlib; print(tomllib.loads(pathlib.Path('openqp/pyproject.toml').read_text())['project']['version'])"
          $engineSha = git -C openqp rev-parse HEAD
          "OQP_VERSION=$ver" >> $env:GITHUB_ENV
          $env:OQP_VERSION = $ver
          $root = "C:\oqpdist\openqp"
          # `import oqp` prints "Failed to import mpi4py" to stdout on a non-MPI
          # build, so take only the last line -- the path.
          $pkg = (python -c "import os, oqp; print(os.path.dirname(oqp.__file__))") | Select-Object -Last 1
          # basis sets / examples ship next to the executable so a user can point
          # at them without hunting inside _internal.
          Copy-Item -Recurse -Force "$pkg\share" "$root\share"
          Copy-Item -Recurse -Force "openqp\examples" "$root\examples"
          $readme = @(
            'OpenQP for Windows -- standalone build'
            '======================================'
            ''
            'Nothing to install.  Unzip anywhere and run, for example:'
            ''
            '    openqp.exe examples\HF\H2O_RHF-HF_ENERGY.inp'
            ''
            'Results are written next to the input file (.log, .json).'
            ''
            'Threads: set OMP_NUM_THREADS to the number of physical cores, e.g.'
            ''
            '    set OMP_NUM_THREADS=8'
            '    openqp.exe myjob.inp'
            ''
            'This bundle carries its own Python, Intel Fortran runtime, Intel MKL'
            'DFT-D4 and external ddX continuum-solvent libraries.  It neither'
            'reads nor needs any system installation of them.  ddX remains'
            'bundled until OpenQP''s native continuum solver is merged.'
            ''
            'Built with Intel oneAPI ifx/icx 2026.1 and Intel MKL (ILP64).'
            ''
            "OpenQP version : $env:OQP_VERSION"
            "OpenQP release : ${{ steps.engine.outputs.tag }}"
            "OpenQP commit  : $engineSha"
            "Built          : $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss') UTC"
            "Built by       : $env:GITHUB_SERVER_URL/$env:GITHUB_REPOSITORY/actions/runs/$env:GITHUB_RUN_ID"
          )
          $readme | Set-Content -Encoding UTF8 "$root\README.txt"
          Get-Content "$root\README.txt" | Select-Object -Last 5

      - name: Smoke test the bundle in a clean shell
        run: |
          $env:PATH = "C:\Windows\System32;C:\Windows"   # no Python, no oneAPI
          New-Item -ItemType Directory -Force -Path C:\oqpsmoke | Out-Null
          Copy-Item openqp\examples\HF\H2O_RHF-HF_ENERGY.inp C:\oqpsmoke\hf.inp
          Set-Location C:\oqpsmoke
          $env:OMP_NUM_THREADS = "2"
          & C:\oqpdist\openqp\openqp.exe hf.inp --nompi
          if ($LASTEXITCODE -ne 0) { throw "openqp.exe exited $LASTEXITCODE" }
          $hit = Select-String -Path hf.log -Pattern "TOTAL energy"
          if (-not $hit) { throw "no TOTAL energy in hf.log" }
          $hit
          # -76.0107465151 is the reference this example carries.
          if (-not (Select-String -Path hf.log -Pattern "-76\.01074651")) {
            throw "energy does not match the reference"
          }
          Copy-Item "$env:GITHUB_WORKSPACE\openqp\examples\PCM\H2O_RHF-HF_DDPCM_ENERGY_ISPHER.inp" C:\oqpsmoke\pcm.inp
          & C:\oqpdist\openqp\openqp.exe pcm.inp --nompi
          if ($LASTEXITCODE -ne 0) { throw "PCM openqp.exe exited $LASTEXITCODE" }
          if (-not (Select-String -Path pcm.log -Pattern "TOTAL energy")) {
            throw "no TOTAL energy in pcm.log"
          }
          if (Select-String -Path pcm.log -Pattern "without OQP_ENABLE_DDX") {
            throw "PCM bundle was built without ddX"
          }

      - name: Zip the bundle
        run: |
          $ver = $env:OQP_VERSION
          # Always a released engine, so the version alone names it; the
          # README inside records the tag, the engine commit and the run.
          $zip = "openqp-$ver-windows-x86_64.zip"
          # Zip the CONTENTS of the frozen directory, so unzipping gives
          # openqp.exe at the top rather than one more folder to descend into.
          Compress-Archive -Path C:\oqpdist\openqp\* -DestinationPath $zip -CompressionLevel Optimal
          "BUNDLE_ZIP=$zip" >> $env:GITHUB_ENV
          Get-Item $zip | Select-Object Name, @{n='MB';e={[math]::Round($_.Length/1MB,1)}}

      - uses: actions/upload-artifact@v4
        with:
          name: openqp-windows-standalone
          path: ${{ env.BUNDLE_ZIP }}
          if-no-files-found: error

      - name: Attach to the release
        if: github.event_name == 'release' || inputs.release_tag != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${{ github.event.release.tag_name || inputs.release_tag }}" $env:BUNDLE_ZIP --clobber
